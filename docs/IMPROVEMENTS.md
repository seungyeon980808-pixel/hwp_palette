# hwp_palette 개선 과제 30선

전체 코드(4,312줄 / 13개 파일)를 정독하고 뽑은 개선 목록.
**우선순위는 "실제로 사용을 막는가"** 기준. 방향 결정용 참고 문서이며, 이 순서대로
할 필요는 없다.

> 표기: 🔴 버그(지금 문제 발생) · 🟠 UX(불편) · 🟡 구조(유지보수) · 🔵 기능(확장)
> [실측] = 실제로 재현·확인함 / [추정] = 코드를 읽고 판단, 미검증
> **✅ = 완료** (2026-07-18 기준: 1, 2, 4, 5, 6, 7, 8, 9, 11, 12, 13, 14, 15, 17, 18, 19,
> 20, 22, 26, 28 — 20개)

---

## A. 지금 실제로 문제가 되는 것 (버그)

### 1. ✅🔴 라이브러리에서 삭제해도 팔레트 블럭이 고아로 남음 [실측]
템플릿 '결재란'을 라이브러리에서 지워도 팔레트 블럭은 그대로 남고, 클릭하면
"템플릿을 찾을 수 없습니다" 오류. 조각 파일은 이미 지워진 뒤라 복구 불가.
→ 삭제 시 참조하는 블럭도 같이 정리하거나, 최소한 경고("팔레트 2곳에서 사용 중").

### 2. ✅🔴 템플릿 이름을 바꾸면 팔레트 블럭이 깨짐 [추정]
블럭이 템플릿을 **이름 문자열**로 참조(`{"template": "결재란"}`). 이름 변경 시
연결이 끊긴다. → 이름 대신 **고유 id**로 참조해야 근본 해결.

### 3. 🔴 라이브러리 항목 이름 중복 처리와 팔레트 참조 충돌 [추정]
같은 이름 등록 시 `결재란 (2)`로 자동 변경되는데, 팔레트 블럭은 원래 이름을
들고 있어 엉뚱한 걸 가리킬 수 있다. (2번과 같은 뿌리)

### 4. ✅🔴 예외를 조용히 삼키는 곳이 23군데 [실측]
`except Exception: pass` — hwp_engine 12곳, palette_ui 3곳 등. 실패해도 사용자는
모르고 "왜 안 되지?"만 남는다. 오늘 겪은 버그들이 오래 숨어있던 이유.
→ 최소한 로그로 남기거나, 사용자에게 알릴 것과 무시할 것을 구분.

### 5. ✅🔴 `\` 청소가 문서 끝까지 훑어 무관한 역슬래시까지 지울 위험 [추정]
`strip_slot_markers(anchor)`가 anchor부터 **문서 끝까지** `\`를 지운다. 템플릿
아래에 사용자가 쓴 `\`가 있으면 같이 사라진다. → 삽입 범위 안으로 제한 필요.

**해결(2026-07-18)**: `strip_slot_markers(anchor, end_para)`로 범위 인자를 받게 함.
삽입물의 끝 문단은 `measure_insert_span()`이 삽입 전후의 '문서 마지막 문단 번호'
차이로 역산한다 — `insert_file` 직후 커서가 조각 뒤로 안 가서(실측) 커서로는
끝을 알 수 없기 때문. 범위를 벗어난 `\`를 만나면 지우지 않고 멈춘다.
실패 방향이 '빈칸이 남는다'(눈에 보임)이지 '남의 글자를 지운다'(안 보임)가
아니도록 잡았다. `end_para=None`은 양식을 새 문서로 연 직후 — 문서 전체가
대상이라 안전한 유일한 경우 — 에만 의도적으로 쓴다.

### 6. ✅🔴 조각 삽입 시 원본 문서의 글꼴/스타일이 대상 문서로 딸려 올 수 있음 [추정]
`insert_file(keep_charshape=1, keep_style=1)` 기본값. 용지 여백은 안 따라오는 걸
확인했지만(실측), 스타일 오염 가능성은 미검증.

**조사 결과(2026-07-18, pyhwpx 소스 확인)**: 전제는 사실이었다. `insert_file`의
기본값은 `keep_section=1, keep_charshape=1, keep_parashape=1, keep_style=1`이고,
우리 코드는 `keep_section=0`만 넘겨 나머지 셋은 암묵적으로 1이었다.

다만 셋의 성격이 다르다:
- `keep_charshape` / `keep_parashape` = 1 은 **필요한 동작**이다. 조각의 글자·문단
  서식이 따라와야 표가 원본대로 재현된다. 끄면 안 된다.
- `keep_style` = 1 만이 우려 대상 — 조각에 딸린 '스타일 정의'까지 들여온다.

값은 바꾸지 않고 **네 인자를 모두 명시**해 코드에 드러나게만 했다. 실제 오염
사례가 관측되지 않은 상태에서 `keep_style=0`으로 바꾸면 조각 모양이 달라지는
쪽의 회귀 위험이 더 크다고 판단. 피해 사례가 나오면 그때 0으로 전환한다.


### 31. ✅🔴 양식 블럭이 메인 팔레트에서만 다른 모양으로 보임 [실측]
*(30선 작성 이후 발견 — 26번으로 양식 기능을 추가하면서 같이 들어간 누락)*

`main._BLOCK_COLOR`에 `"form"` 키가 없고 `_make_block_button`의 분기도 form을
`else`(기능 블럭)로 흘려보냈다. 그래서 같은 블럭이 두 곳에서 다르게 보였다:

| | 환경설정 미리보기 | 메인 팔레트 (버그) |
| --- | --- | --- |
| 기호 | `📄` | `ƒ` (기능 블럭 기호) |
| 배경 | 연녹색 `#eafaf1` | 흰색 (폴백) |

**해결(2026-07-18)**: `_BLOCK_COLOR`/`_BLOCK_PREFIX`에 form 추가 + `_block_label()`
분리. 템플릿과 마찬가지로 라이브러리의 '현재' 이름을 따라가므로 이름을 바꿔도
팔레트에 반영된다. 두 곳의 색·기호가 어긋나면 또 생길 문제라 주석으로 못 박아 뒀다.

---

## B. 사용자를 헷갈리게 하는 것 (UX)

### 7. ✅🟠 "저장은 라이브러리, 배치는 환경설정" — 창이 갈라져 있음
템플릿 하나 쓰려면 창 2개, 단계 8개. 실제로 "템플릿 저장 어디 갔지?"를 겪음.
→ 환경설정의 `+템플릿`에서 **바로 캡처** 가능하게. (가장 큰 개선)

### 8. ✅🟠 등록할 때 이름·라벨·분류 3칸을 매번 물어봄
라벨은 99% 이름과 같고, 분류는 처음엔 불필요. `\계획서표지\` 사태의 원인.
→ **이름만** 묻고, 라벨·분류는 "▸자세히"로 접기.

### 9. ✅🟠 라이브러리 항목을 수정할 수 없음
등록하면 삭제 후 재등록뿐. 팔레트 블럭은 더블클릭으로 고쳐지는데 라이브러리는
안 되니 일관성도 없다. → 더블클릭 편집 + 템플릿 "다시 캡처".

### 10. 🟠 용어가 겹쳐서 헷갈림
라이브러리/템플릿/블럭/서식/기능/양식/팔레트/탭. 특히 **"서식"(라이브러리)과
"기능"(팔레트)이 사실상 같은 것**. → 통합하거나 이름을 갈라야.

### 11. ✅🟠 `\`가 두 가지 뜻 (빈칸 / 호출)
"감싸면 호출, 안 감싸면 빈칸" 규칙을 외워야 한다. 실제로 사용자가 `/`로 잘못
입력한 사건 발생. → 등록창에 **미리보기**("이렇게 쓰세요: `\보기\`")를 보여줄 것.

### 12. ✅🟠 실패해도 왜 실패했는지 안 알려줌
"기능은 글자를 선택한 뒤 눌러주세요" 정도는 있지만, 대부분은 상태표시줄에
한 줄 스치고 끝. → 실패 사유를 명확히.

**해결(2026-07-18)**: `main.report_error(what, error)` 하나로 통일. 실패를 세 곳에
동시에 남긴다 — app.log(나중에 원인 추적) + 메시지박스(지금 알아야 함) +
상태표시줄(메시지박스를 닫은 뒤에도 남음). 예전엔 `messagebox.showerror("오류", ...)`
라 제목만 봐서는 무슨 작업이 실패했는지도 알 수 없었다. 이제 "마크다운 변환 실패"
처럼 작업 이름이 들어간다. 변환·블럭 실행은 단계가 많아 `detail=True`로 스택까지
남긴다.

### 13. ✅🟠 드래그 배치가 "타일 위에 놓기"만 됨
빈 칸에 직접 놓을 수 없고, 드래그 중 미리보기(어디 놓일지)가 없다.
사용자가 원한 "빈 공간에 블럭을 하나씩" 은 미구현.

### 14. ✅🟠 블럭이 많아지면 격자가 창 밖으로 넘침
스크롤이 없다. 빠른입력 25개 + 추가하면 화면을 벗어난다.

### 15. ✅🟠 팔레트 블럭 이름이 12자에서 잘림
`▦ 계획서표지…` 처럼 잘려서 뭐가 뭔지 구분이 안 될 수 있다. → 툴팁 필요.

**해결(2026-07-18)**: `main._add_tooltip()` 추가 — 이름이 실제로 잘렸을 때만 붙는다
(안 잘린 블럭에 말풍선이 뜨면 성가시다). 메인 창이 topmost라 말풍선에도
`-topmost`를 줘야 뒤로 안 숨는다.

### 16. 🟠 실행 취소(Undo)에 대한 안내가 없음
템플릿을 잘못 꽂았을 때 한글의 Ctrl+Z로 되돌아가는지 불명확. → 확인 후 안내.

---

## C. 구조·유지보수

### 17. ✅🟡 죽은 코드 4개 [실측]
`run_action`, `insert_marked_choice`, `set_markpen`, `apply_reset_format` — 호출 0곳.
원문자/형광펜 팔레트를 걷어내며 남은 잔해. → 삭제.

### 18. ✅🟡 `quickbuttons_ui.py`(112줄)가 통째로 미사용 [실측]
빠른입력이 팔레트 탭으로 흡수되며 아무도 import 안 함. → 삭제.

### 19. ✅🟡 `hwp_engine.py`가 986줄로 비대
연결·텍스트·서식·표·시험문제·캡처·변환이 한 파일에. → 최소 3개로 분리
(`engine_core` / `engine_exam` / `engine_library`).

**해결(2026-07-18)**: 3분할 완료. 파일명은 `engine_core` 대신 `hwp_engine`을 유지했다
— 이미 9개 파일이 참조하고 있어 이름만 바꾸는 변경으로 diff를 키울 이유가 없다.

| 모듈 | 역할 |
| --- | --- |
| `hwp_engine.py` | 연결·문서·선택·글꼴·표 생성·찾기 (다른 엔진이 공유하는 토대) |
| `exam_engine.py` | 시험문제 조판 (20번에서 이미 분리) |
| `engine_library.py` | 라이브러리 캡처/적용, 팔레트 블럭 실행, `\라벨\` 변환 실행 |

주의점: 연결 인스턴스(`hwp`)는 `hwp_engine`이 소유하고 재연결 때 새 객체로 바뀐다.
그래서 다른 모듈은 반드시 `hwp_engine.hwp`로 참조해야 한다 —
`from hwp_engine import hwp`로 가져오면 재연결 후 낡은 객체를 붙들게 된다.
`engine_library`는 `_h()` 접근자로, `exam_engine`은 `_act()/_ps()`로 이 규칙을 지킨다.

### 20. ✅🟡 시험문제 전용 코드가 엔진 절반을 차지
`insert_bogi_box`, `insert_material_box`, `insert_photo_box`, `insert_experiment_box`,
`insert_question`, `_insert_choices` — 프로그램이 '팔레트'로 성격이 바뀐 지금,
이것만 별도 모듈로 빼는 게 맞다.

### 21. 🟡 설정이 두 군데로 갈라짐
`settings.py`(양식 프리셋)와 `palette.py`(탭·기본서식)가 같은 config.json을 쓰지만
API가 따로. → 하나로 정리하거나 역할을 명확히.

### 22. ✅🟡 테스트가 하나도 없음
`parser.build_library_plan` 같은 순수 함수는 한글 없이도 테스트 가능한데 없다.
→ 최소한 파서·라이브러리·팔레트 모델에 단위 테스트.

**해결(2026-07-18)**: `tests/test_parser.py` — 28개. 실행은 `python -m unittest discover -s tests`.
한글 없이 검증 가능한 경계(파싱 계층)만 덮었다. 엔진은 한글을 띄워야 해서 제외.

테스트를 쓰다가 버그를 하나 찾았다: 양식 라벨을 줄 중간에 쓰면 등록돼 있는데도
"등록되지 않은 라벨"이라고 알려줬다(`parser._replace_char_tokens`가 '양식' 분류를
처리 안 함). 이제 "양식 라벨은 한 줄에 단독으로 써주세요"로 나온다.

### 23. 🟡 매직넘버가 코드에 흩어져 있음
`_CELL_SIDE_MARGIN_MM = 3.6`, `0x2FFF1`, `range(200)`, `12자 자르기` 등.

### 24. 🟡 `import`가 함수 안에 들어간 곳들
`_diag`, `connect`, `_add_template` 등에서 지역 import. 의도(선택적 의존성)는
이해되나 일관성이 없다.

### 25. 🟡 버전 문자열이 실제와 안 맞음
`VERSION = "1.3.0"`인데 그 뒤로 라이브러리·팔레트·마크다운 변환이 통째로 들어옴.
docstring의 버전 이력도 v1.3.0에서 멈춤. → 정리 필요(사용자 지시 시).

---

## D. 기능 확장

### 26. ✅🔵 양식(.hwp 파일) 등록 — 완료
파일을 골라 등록 → 클릭하면 **새 문서로 열기**. 표지·통신문처럼 "이 양식으로
시작"하는 경우에 필요. 삽입은 페이지 설정이 안 따라오므로 별도 종류가 맞다.

### 27. 🔵 서식 감싸기 문법 (`\굵게\내용\/`)
현재 `\라벨\`은 문자·템플릿만. 서식은 팔레트 클릭으로만 적용 가능.
닫는 표시를 정해야 함(빈칸 `\`와 충돌하지 않게 `\/` 등).

### 28. ✅🔵 자간 자동 조절로 어절 단위 줄바꿈 — 완료
사용자 요청. 한글에 "한글 단위/어절 단위 줄나눔" 문단 설정이 있는지,
자간 자동 조절이 API로 되는지 확인 필요.

### 29. 🔵 템플릿 빈칸에 서식 미리 지정
"이 빈칸에 들어갈 글자는 굵게 11pt" 를 템플릿에 심어두기.
현재는 빈칸 위치의 서식을 그대로 물려받는다. → 실제로 되는지 확인 필요.

### 30. 🔵 라이브러리 내보내기/가져오기
양식 프리셋에는 있는데(`export_profile`) 라이브러리엔 없다.
폴더째 복사는 되지만, 동료와 **항목 단위 공유**가 안 된다. README에
"폴더째 복사하면 이식 가능"이라 써뒀으니 정식 기능으로 만들 가치 있음.

---

## 추천 순서 (내 의견)

**1단계 — 지금 아픈 것**: ✅ 완료 (1, 2, 7, 8, 9, +11)
**2단계 — 청소**: ✅ 완료 (17, 18, 20 — hwp_engine 986→656줄, exam_engine.py 분리)
**3단계 — 합의된 기능**: ✅ 완료 (26 — 라이브러리 4번째 종류로 추가)
**4단계 — 나머지**: ✅ 4, 13, 14, 28 완료
**5단계 (2026-07-18)**: ✅ 5, 6, 12, 15, 19, 22 + 31(신규) 완료

**남은 것**: 3, 10, 16, 21, 23, 24, 25, 27, 29, 30

> 30개를 다 할 필요는 없다. **1·2·7번만 해결해도 체감의 절반 이상**이다.

### 남은 것에 대한 판단 (2026-07-18)
- **3번**은 2번(id 참조 전환)으로 원인이 사라졌을 가능성이 높다. 재현되지 않으면
  목록에서 지우는 게 맞다.
- **10번**(용어 중복)은 UI 문구·저장 키·문서를 동시에 건드려야 해서 비용 대비
  효과가 가장 나쁘다. 우선순위 최하위.
- **27·29·30**은 버그 수정이 아니라 신규 기능이라 별도 작업으로 다룬다.
- **25번**(버전 문자열)은 의도적 보류 — 버전은 사용자가 명시적으로 지시할 때만
  올린다(CLAUDE.md 규칙).


---

## 4번 관련 메모 (2026-07-17)

`applog.py` 를 만들어 **조용히 삼켜지던 실패를 app.log 에 기록**하게 했다.
전부를 한 번에 바꾸면 위험해서, **실제로 버그가 숨었던 지점부터** 적용했다:

- `connect()` 창 배치 저장·복원 실패 → 창을 0개로 봤던 대소문자 버그가
  아무 소리 없이 무동작이었던 그 자리
- `settings.load_config/save_config` → 설정이 깨지거나 저장 실패하면
  사용자 팔레트가 통째로 사라진 것처럼 보이는데 조용했음
- `palette._migrate_template_ref` → 마이그레이션 실패 시 연결이 다 끊김

남은 `except: pass` 는 대부분 "실패해도 무해한" 곳(위젯 config 등)이라
급하지 않다. 필요하면 같은 방식으로 확장하면 된다.
